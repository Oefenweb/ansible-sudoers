# defaults file
---
sudoers_sudoers:
  defaults: "{{ sudoers_sudoers_preset_defaults }}"
  privileges: "{{ sudoers_sudoers_preset_privileges }}"

sudoers_sudoers_d_files: {}

sudoers_include_variables: true
sudoers_install: true
