# test file for sudoers
---
- hosts: all
  remote_user: vagrant
  sudo: true
  roles:
    - sudoers
  vars:
    sudoers_sudoers_d_files:
      travis:
        privileges:
          - name: travis
            entry: "ALL=NOPASSWD:ALL"
